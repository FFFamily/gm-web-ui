# Production
# If frontend and backend are on different domains, set backend origin here, e.g.:
# VITE_API_BASE_URL=https://api.example.com
#
# If you use Nginx same-origin proxy for `/api/*` and `/files/*`, you can keep it empty.
VITE_API_BASE_URL=

